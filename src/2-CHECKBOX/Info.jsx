const Info = () => {
  return (
    <div>
      <h1 className="heading">Project Name:Tranfering data in Checkboxs</h1>
      <div className="features">
        <span className="set">
          Features:This project transfer the checkboxes from one table to
          another from source to targe by clicking the button . You can see the
          code in above repository but straight go the 2-CHECKBOX folder which
          contains the code
        </span>
      </div>
    </div>
  );
};

export default Info;
